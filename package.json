{
  "name": "wasm-canvas-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "An infinite drawing canvas where every piece of UI — the canvas included — is a live query over an in-tab wasm IVM engine: drag = writes, views fold before the frame paints, and the canvas itself is one live query per visible cell.",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --test test/*.test.ts && node test/differential.e2e.ts",
    "test:browser": "vite build && node test/browser-smoke.mjs"
  },
  "dependencies": {
    "@rindle/client": "0.10.2",
    "@rindle/wasm": "0.10.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vite": "^8.0.0"
  }
}
